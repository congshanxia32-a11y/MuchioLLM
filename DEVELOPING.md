# ムチォLLMコンパニオン — 改造用メモ

> 開発者向けの内部メモです。**インストール・使い方は [README.md](README.md) へ。**

VRCペット「ムチォ」(VRCPet.exe) を Ollama で会話させる常駐システム。
**VRCPet・Unityプロジェクト・アバターはすべて無改造**。ここのファイルだけで完結している。

> VRChatで使うときは `VRCPet.exe` を起動しないこと。MuchioLLMと競合してVRChat側が壊れるため、通常は `--mic` リスナーを使う。

次に触るときに時間を溶かさないよう、**調べ直すのが大変だったこと**を中心に残す。

---

## 1. 全体の流れ

```
[あなたの声]
  ├→ VRCPet.exe ─→ %APPDATA%\VRCPet\data\logs\日付.jsonl ─┐
  └→ vrc_listener.py --mic ─→ data/owner_heard.jsonl ────┤
                                                          ├→ muchio_llm.py
[フレンドの声(VRChatの音声出力)]                            │    (返答生成)
  └→ vrc_listener.py ─→ data/others_heard.jsonl ─────────┘         │
                                                                    ↓
                                          OSC(127.0.0.1:9000) → KAT文字盤
```

- VRCPet が動いていれば **あなたの声はVRCPet経由**（純正のひとりごと・単語学習も生きる）
- VRCPet が無くても **`--mic` リスナー**が拾うので単体で動く
- VRCPet 起動中は `--mic` が**自動で休止**する（二重認識の防止、10秒ごとに tasklist で検出）

## 2. ファイル

| ファイル | 役割 |
|---|---|
| `muchio_llm.py` | 本体。ログ監視・LLM呼び出し・KAT送信・設定UI(http://localhost:8787) |
| `vrc_listener.py` | 音声→文字起こし。ループバック(フレンド) と `--mic`(飼い主) の2役 |
| `run.bat` | 上記3プロセスを起動。**古いインスタンスを自動killしてから起動する** |
| `kat_sniff.py` | 検証用。VRChatを閉じてUDP9000を受信し、VRCPetの盤面出力を復元表示 |
| `config.json` | 設定。UIから保存され、本体がmtime監視でホットリロード（再起動不要） |
| `data/conversation.jsonl` | 会話の全記録(日本語界隈)。UIのリセットで `.bak` に退避される |
| `data/conversation_en.jsonl`・`data/diary_en.jsonl` | 英語界隈の会話・日記。界隈(mode)がenのときはこちらに読み書きされ、記憶が日本語界隈と混ざらない |
| `data/muchio_llm.log` | 動作ログ |

CLI:
```
python muchio_llm.py                  # 常駐
python muchio_llm.py --say てすと       # 文字盤に直接表示（プロトコル確認用）
python muchio_llm.py --ask こんにちは    # LLM返答だけ生成（送信しない）
python vrc_listener.py --list          # キャプチャできるデバイス一覧
python vrc_listener.py --mic AG06      # マイク指定
```

## 3. KAT文字盤プロトコル（調べ直さないこと）

本家 [killfrenzy96/KatOscApp](https://github.com/killfrenzy96/KatOscApp) の `katosc.py` と同一。
Unityアセット(`Assets/Muchio/gimic/`)の静的解析で確定済み。

- `KAT_Pointer`(int) にブロック番号 **1〜8** を送り、続けて `KAT_CharSync0〜7`(float) に8文字ぶん送る
- セル番号 = `(Pointer-1)*8 + Sync番号`
- **64セル(32桁×2行)が物理上限**。シェーダーは128セル持つが、ムチォのアニメーターに
  Pointer9-16のステートが無い。128字化するにはUnity側(FXコントローラー)の改造が必要
- float値 = `idx<=127 ? idx/127 : (idx-256)/127`
- 文字テーブルは本家表 + ムチォ改変フォント分（`っ`=96, `ッ`=97, `、`=99）
- **フォントに「ー」が無い**ので「-」(idx13)に変換している。漢字も出せない
- ブロック間の送信間隔は 0.25秒（本家準拠）。ただし**同一ブロック内の文字送りは遷移が要らない**
  （アニメーターの遷移が duration 0 / no exit time なので即時反映）

### 全消去の罠
`KAT_Pointer=255` でクリアステートに入るが、**クリアアニメは Motion Time = CharSync値で
サンプリングされる**。なので `255` だけ送ると、その時のCharSync値が全セルに塗られてゴミが出る。
**必ず全CharSync0-7=0.0 とセットで送ること**（本家の初期化と同じ手順）。

### 純正との共存
VRCPetは差分同期（自分が書いた覚えのないセルは触らない）なので、こちらが書いたセルは
VRCPetには消せない。**表示したら必ずクリアして返す**こと。
衝突回避は時間ベースのみ（純正のsaid検知後6秒は送信を保留）。
これで足りなくなったら **単一ライター化**（VRChatを `--osc=9002:127.0.0.1:9001` で起動して
VRCPetの盤面出力を無効化し、こちらが純正セリフも代行表示する）に移行する。

## 4. 踏んだ罠（同じ穴に落ちないため）

### Ollama
- **`num_ctx` を必ず明示する**。省略すると ollama が空きVRAMを見て巨大コンテキスト
  （qwen3は最大256k）を確保しに行き、KVキャッシュだけで10GB超・ロードが118秒〜無限になる。
  `num_ctx: 8192` を指定したら **0.3秒**になった。実際の入力は2千トークン以下
- **思考型モデルは `"think": false` を送る**。qwen3:32b等は既定で思考モードがオンで、
  出力が丸ごと `<think>` に入り、除去した結果**空返事**になる（トークン上限も思考で使い切る）。
  非対応モデルは400を返すので、その場合だけ `think` を外して再試行する実装になっている
- モデルがRAMに載らないと ollama サーバーごとクラッシュする（235b=142GB vs RAM 137GBで実際に落ちた）。
  UIのドロップダウンはサイズ警告を出すが、過信しないこと
- **VRAMの実測は `nvidia-smi` では取れない**（Windowsはプロセス別VRAMを出さない）。
  性能カウンタ `\GPU Process Memory(*)\Local Usage` を使う

### 日本語処理
- **pykakasi は絵文字が混ざると直前の語を複製する**。
  `'不思議だね📐🔍'` → `'ふしぎだねだねだね'`。
  **変換前に必ず絵文字・非日本語文字を落とす**こと(`_EXOTIC_RE`)
- モデルが返事のあとにルールの自己解説を書くことがある
  （「（最後の絵文字は指示に基づき削除します）」等）→ 1行目だけ採用 + メタ以降を切り捨て

### LLMの口調
- **人格文の単語はそのままアトラクタになる**。「たまに**蒸し返す**」と書いたら全返答が
  「**また**◯◯だね!」型に収束した。抽象語より「まいかい言い回しを変える」のような
  指示のほうが効く
- **履歴は自己強化する**。悪い形式の返答が溜まると毎回お手本になって抜け出せない。
  対策として (a) LLMに渡す**自分の過去返答は直近3件だけ**、(b) difflib類似度0.62で重複判定
  （完全一致だと「あ-/あっ」の揺れがすり抜ける）、(c) UIに会話リセットボタン
- ただし**定期リセットは不要**。文脈は構造的に上限（読み込み40件・保持80件・投入40件）が
  かかっているので、jsonlが何万行に育っても壊れない

### 音声認識
- **短い発話は言語判定が当てにならない**。「むちこ」が韓国語判定されて捨てられていた。
  高確度(>0.7)の外国語だけ除外し、低確度は日本語扱いで通している。
  日本語/英語モードでは `language` を固定するのが一番効く
- **whisperの名前の聞き間違いは正規表現で吸収**する
  （眠ちこ/ムーチコ/めちこ/むっちこ/町子/のち子… → `[まむめもの][ぁ-ゖー]?ち[ぁ-ゖー]?こ`）
- **VRで遊ぶときはデバイスが変わる**。Quest経由の声は「マイク (Virtual Desktop Audio)」、
  フレンドの声は「スピーカー (Virtual Desktop Audio)」に出る。run.bat の `MIC`/`SPK` に
  `"Virtual Desktop"` を書く。デスクトップ時は空でよい
- リスナーは15秒ごとに音量RMSをログに出す。**話してもRMSが上がらない＝デバイス違い**
- GPU whisperには `nvidia-cublas-cu12` と `nvidia-cudnn-cu12` が必要
  （`site-packages/nvidia/*/bin` を `os.add_dll_directory` で通している）

### Windows
- **同じポートに二重bindできてしまう**。設定UIは `allow_reuse_address = False` で塞いだ。
  run.bat も起動時に旧プロセスをkillする
- `tasklist` の出力はCP932。**デコードせずバイト比較**すること（UnicodeDecodeErrorになる）
- 設定UIのフォームは開いた時点の値を持つので、**開きっぱなしで保存すると古い値で巻き戻る**。
  hidden の `cfg_mtime` で検出して409で拒否している

## 5. 設定(config.json)

UIから編集する前提。全部ホットリロードされる（本体は毎ループ、リスナーは15秒ごと）。

| キー | 意味 |
|---|---|
| `mode` | `auto`/`jp`/`en`。モデル・プロンプト・**音声認識の言語ロック**が一括で変わる |
| `model` / `model_en` | 通常モデル / 英語モード用モデル |
| `reply_chance` / `friend_reply_chance` | 名前を呼ばれていない発話に反応する確率 |
| `cooldown` | 名前なし反応の最短間隔（名前を呼ばれたら無視して必ず返す） |
| `idle_seconds` | ひとりごとの間隔（0=しない）。±ゆらぎ付き |
| `typing_speed` | 1文字ずつ表示する速さ(秒/文字)。0=一括 |
| `max_reply` | 返答の切詰め文字数（**盤面の物理上限は64**） |
| `persona` | 人格文。ひらがな縛り等の基本ルールは自動で付く |
| `rms_gate` / `silence_end` | リスナーの音量ゲート / 発話終了判定 |
| `stt_hint` / `stt_hint_en` | 音声認識のヒント。固有名詞を入れると認識が安定する |

## 6. 動作確認のしかた

```bash
# 文字盤だけ確認（LLMを通さない）
python muchio_llm.py --say てすと

# LLMだけ確認（OSCを送らない）
python muchio_llm.py --ask むちこ、おなかすいた

# プロトコルの検証（VRChatを閉じてから。VRCPetの出力を復元表示する）
python kat_sniff.py
```

デーモンのログの読み方:
- `純正: 〜` … VRCPet連携が生きている証拠
- `きいた(owner/friend, 返答へ/スルー): 〜` … 認識できた発話
- `へんじ: 〜 (raw: …)` … 後処理の前後が見える。**rawと表示がずれていたら後処理のバグ**

## 7. 意図的にやっていないこと（拡張の候補）

- **真のトークンストリーミング**（生成しながら1文字ずつ出す）。漢字→ひらがな変換が
  部分文字列で揺れる（「生」→「せい」が「生きてる」で「いきてる」に変わる）のと、
  重複チェックが表示後にできないため見送り。やるなら末尾が漢字の間は表示を保留する方式
- **長期記憶の要約**。いまは直近40件を渡すだけ。会話が深くなったら要約注入を足す
- **128文字化**（Unity側のFXコントローラーにPointer9-16のステート追加が必要）
- **VR/デスクトップの自動判別**。VD Streamerは常駐しているので「いまVR中か」の信号にならない。
  やるなら pycaw で音声セッションを監視する
- **モデル切替の自動化**（発話言語ごとにモデルを変える）。切替のたびに10秒級のロードが
  走るので現実的でない。モードは手動切替のままが良い
